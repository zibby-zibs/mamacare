import React from "react";
import UserDetailPage from "../_components/single-user";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <UserDetailPage />
    </div>
  );
};

export default page;
