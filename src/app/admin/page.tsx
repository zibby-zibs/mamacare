import React from "react";
import HomePage from "./_components/home";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <HomePage />
    </div>
  );
};

export default page;
