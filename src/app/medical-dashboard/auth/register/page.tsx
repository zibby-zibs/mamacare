import React from "react";
import SignupPage from "../../_components/register";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <SignupPage />
    </div>
  );
};

export default page;
